sourceSets {
    generator
}

tasks.register("generate", JavaExec) {
    classpath sourceSets.generator.runtimeClasspath
    jvmArgs += "--enable-preview"
    mainClass = "org.overrun.glib.gl.OpenGLGenerator"
    workingDir = file("src/main/java/org/overrun/glib/gl")
}
