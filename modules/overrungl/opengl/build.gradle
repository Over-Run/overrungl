sourceSets {
    generator
}

tasks.register("generate", JavaExec) {
    classpath sourceSets.generator.runtimeClasspath
    jvmArgs += "--enable-preview"
    mainClass = "org.overrun.gl.opengl.OpenGLGenerator"
    workingDir = file("src/main/java/org/overrun/gl/opengl")
}
