sourceSets {
    generator
}

tasks.register("generate", JavaExec) {
    classpath sourceSets.generator.runtimeClasspath
    jvmArgs += "--enable-preview"
    mainClass = "overrungl.opengl.OpenGLGenerator"
    workingDir = file("src/main/java/overrungl/opengl")
}
